version: '3.8'

services:
  web:
    build: .
    container_name: myaiassist_container
    ports:
      - "5555:5000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    restart: unless-stopped
